import React, { Component } from 'react';
import WeatherList from '../containers/SomeComponent';

export default class App extends Component {
  render() {
    return (
      <div>
        <SomeComponennt />
      </div>
    );
  }
}
